{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "nodeapp.fullname" .) }}
  labels:
    {{- include "nodeapp.labels" . | nindent 4 }}
{{- end }}
